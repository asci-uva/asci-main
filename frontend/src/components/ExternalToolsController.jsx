import React from "react";
import Home from "./externalTools/Home";

const ExternalToolsController = (props) => {
  return (
      <Home {...props} />
  );
};

export default ExternalToolsController;
